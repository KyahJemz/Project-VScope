// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com", "i.imgur.com", "lh3.googleusercontent.com"],
    },
};

module.exports = nextConfig;
